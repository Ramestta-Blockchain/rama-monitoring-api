import type { Response, NextFunction } from "express"
import { asyncMiddleware } from "../../../../middlewares/asyncMiddleware"
import { Request } from '../../../../middlewares/authMiddleware'

export const getCheckpointById = asyncMiddleware(async (req: Request, res: Response, _next: NextFunction) => {
    try {

    } catch (error) {

    }
})

export const getAllcheckpoint = asyncMiddleware(async (req: Request, res: Response, _next: NextFunction) => {
    try {

    } catch (error) {

    }
})